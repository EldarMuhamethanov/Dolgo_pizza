<header>
  <div class="row justify-content-between">
    <h1>
      <a href="/">
      🍕Долго пицца
      </a>
    </h1>
    
    <div class="authorization">
      {% if is_granted('ROLE_ADMIN') and page == 'menu' %}
        <button type="button" class="registration" data-toggle="modal" data-target="#myModal">
          Добавить новую пиццу
        </button> 
      {% endif %}
      {% if (page == 'menu' or page == 'login') and not is_granted('ROLE_ADMIN')  %}
        <a href="/registration"><button class="registration">Регистрация</button></a>
      {% endif %}
      {% if page == 'menu' or page == 'registration' %}
        {% if  not is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="/login"><button class="login">Вход</button></a>
        {% endif %} 
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{ path('logout') }}"><button class="login">Выход</button></a>
        {% endif %}
        
      {% endif %}
    </div>
  </div>
</header>